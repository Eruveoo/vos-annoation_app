# Usually leave this file unused — frontend defaults to http://127.0.0.1:12212
# and you use a terminal tunnel instead (see scripts/ on the repo).
#
# Optional override only if you really want a direct remote URL in the browser:
# VITE_API_URL=https://xxxx.lhr.life
